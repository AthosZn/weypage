server:
  port: 8001
spring:
  application:
    name: oauth-service
  jpa:
    database: mysql
    generate-ddl: false
    show-sql: true

#注册中心
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
encrypt:
  fail-on-error: false


